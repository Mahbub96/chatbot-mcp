"""Routing package for gateway behavior.

Keep all routing-related decisions (model selection, tool triggers, task routing)
inside this package so `gateway/main.py` stays focused on HTTP concerns.
"""

